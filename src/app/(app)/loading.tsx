// แสดงโครงหน้าทันทีระหว่างรอข้อมูล โดยเมนูยังใช้เปลี่ยนหน้าอื่นได้
// Show a loading shell while data arrives, keeping navigation usable.
export { PageLoading as default } from "@/components/shared/page-loading"
